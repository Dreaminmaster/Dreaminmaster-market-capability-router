# v0.2 Coding Handoff

Implement `docs/V0.2_IMPLEMENTATION_SPEC.md` phase by phase.

Before editing:

```bash
git pull origin main
make all
make openminis-smoke
```

Read `AGENTS.md`, the v0.2 specification, `plan/task_list.md`, and `docs/V0.1.1_VALIDATION_SUMMARY.md`.

Keep rules mode offline and default. Hybrid mode may enrich deterministic output but cannot remove critical risks or required SELF and OFFICIAL routes. Do not add screenshots, browser automation, scraping, a graphical interface, or live-provider dependencies in automated tests.

After each phase, run focused tests and the full legacy suite, commit a coherent change, and record the commit SHA. The final report must include test output, evaluation output, offline fallback, model-conflict safety checks, OpenMinis smoke checks, and a clean working tree.
