# Evaluations

`core_cases.json` contains product-level expectations rather than exact prose snapshots. Evaluation checks routing, friction, market decision, dialect interpretation, query terms, and safety thresholds.

Add a case whenever a product rule changes. Do not replace meaningful cases with implementation-specific unit tests.
